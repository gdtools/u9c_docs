
# 功能描述

设置总账的参数。

# 业务规则

*  启用总账（必做）

# 栏目说明

|栏目名称|说明|
|---|---|
|默认操作账簿|用户选择：默认空|
|新增凭证的制单日期|指定新增凭证时，制单日期是登录日期还是上个凭证日，,默认登录日|
|凭证日期和记账期间严格匹配|勾选后，确定凭证制单日期必须在记账期间内|
|自动携带上条分录摘要|指定新增凭证分录时，是否自动携带上条分录的摘要。默认是|
|自动携带上条分录平衡段|指定新增凭证分录时，是否自动携带上条分录的平衡段。默认是|
|自动携带上条分录科目段|指定新增凭证分录时，是否自动携带上条分录的科目段。默认是|
|自动携带上条分录普通段|指定新增凭证分录时，是否自动携带上条分录的普通段。默认否|
|自动携带上条分录描述性弹性域|指定新增凭证分录时，是否自动携带上条分录的描述性弹性域。默认否|
|自动携带上条分录数量金额信息|指定新增凭证分录时，是否自动携带上条分录的数量金额信息。默认否|
|自动携带上条分录币种|指定新增凭证分录时，是否自动携带上条分录的币种。默认否|
|是否凭证汇总打印|确定是否凭证汇总打印。默认否|
|凭证科目打印方式|指凭证打印时，科目的打印方式。可以选择只打印编码、只打印名称或者打印编码和名称。V2.0的版本中，不能进行选择，必须要编码和名称都打印。这样就导致了科目栏中显示不下。所以增加了这个参数让用户进行选择。|
|科目编码打印方式|打印全科目末级编码、打印全科目首末级编码、打印全科目全部级编码、打印非零段末级编码、打印非零段首末级编码、打印非零段全部级编码|
|科目名称打印方式|打印全科目末级名称、打印全科目首末级名称、打印全科目全部级名称、打印非零段末级名称、打印非零段首末级名称、打印非零段全部级名称|
|凭证自动匹配打印模板|勾选后，凭证批量打印时，自动匹配合适的模板，默认为否|
|是都一张纸打印一张凭证|勾选后，凭证批量打印时，一张纸打印一张凭证，默认为否|
|明细账批量打印时，是否显示总页码|勾选后，批量打印明细账时，显示总页码，默认为否|
|会计主管|勾选后，打印时显示会计主管，默认为否|
|单据任何状态私有段可改|勾选后，单据在任何状态下私有段可修改|
|利润表是否显示补充材料|如果设置为是，显示补充资料，如果设置为否，则不显示补充资料|
|凭证保存时弹出现金流量项目对话框|勾选后，未输入现金流量项目是否弹出现金流量项目对话框。|
|银行科目默认结算方式|选择银行类科目的默认结算方式|
|现金科目默认结算方式|选择现金类科目的默认结算方式|
|总账生成现金银行单据|勾选后，凭证上科目若是现金类或者银行类科目，在审核后，会自动生成现金银行的收付款单。|
|更正凭证直接提交审核记账|确定更正凭证保存后，是否直接提交审核记账。注：如果不符合直接记账的条件，且选择了这个参数，系统自动生成已审核的凭证，用户手工进行记账的操作。例如需要出纳签字的凭证，进行更正后，没有出纳的签字无法记账。此时系统处理成已审核的凭证，用户手工进行记账。|
|是否支持总账凭证登记现金日记账|确定总账凭证是否审核后登记现金日记账|
|是否支持总账凭证登记银行日记账|确定总账凭证是否审核后登记银行日记账|
|凭证提交时检查现金流量|检查需要录入现金流量项目的凭证，在提交时是否录入了现金流量项目|
|凭证双方都是现金流量类科目是否进行现金流量分析|V2.0版本中，默认凭证中只要包括现金流量类科目都需要进行现金流量分析。V2.0sp1调整为根据参数进行判断，如果凭证双方都是现金流量类的科目是否需要进行现金流量分析。参数默认为T。如果选择F,认为此类凭证不进行现金流量分析，但是可以手工勾选进行分析，此时分析的现金流量项目为附表项目。|
|BIP凭证生成现金银行单据|费控云与U9c融合时，勾选后，生成现金银行单据|
|BIP凭证支付时默认银行账号|费控云与U9c融合时，录入支付银行账号|
|外部凭证对接是否同步导入现金流量信息|如果设置为是，直接导入外部凭证的现金流量，如果为否，自动匹配现金流量模板|
|凭证修改室自动更新凭证号|勾选时，当凭证修改了凭证类型、期间等流水依据段时，或者当编号规则发生了变更，然后修改保存凭证时，自动更新凭证号|
|出纳签字需在凭证审核前|勾选后，现金银行科目凭证需要出纳签字的时机：必须在审核前出纳签字|
|外币凭证修改本币金额反算原币金额|外币凭证修改本币金额系统是否根据汇率反算原币金额|
|OBA导入凭证时凭证控制|勾选后，OBA导入凭证时严格校验数量、单价、金额关系|
|"包含未记账"凭证范围,仅包含已审核未记账的凭证|账表查询,函数取数时,此参数打勾，则只包含已核准凭证;不打勾则包含开立、核准中、已核准凭证|
|外部凭证生成是否自动审核|勾选后，外部凭证生成后自动审核|
|来源凭证已经转换允许删除|来源凭证删除会自动删除开立和核准中的目标凭证。如果目标凭证为核准或记账：该参数为是，来源凭证删除，目标凭证保留；如果为否，来源凭证不允许删除|
|结账检查凭证断号|结账时检查凭证是否有断号|

[]()

# 按钮说明

# 操作说明

下发

*  该操作的主要目的是指定那些参数下发到那个组织中；
*  左侧的列表中可以指定针对那些参数进行下发；
*  右侧的组织列表中，可以参照出与当前核算组织有垂直隶属关系的组织列表；
*  在“下发历史”中，可以查看那些参数曾经下发到那些组织，并显示对应的操作人和操作时间；

下发参数

*  在这个操作中，可以指定参数下发后，在下发的组织中能否进行修改；
*  如果是数值型的，还可以指定上浮＼下调的幅度。例如转总账事务数和利息容差的范围；
