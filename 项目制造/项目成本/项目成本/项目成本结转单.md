
# 功能描述

用于结转项目成本；

# 业务规则

## 使用前提

*  启用项目成本模块

# 栏目说明

|栏目名称|说明|
|---|---|
|表头|
|单据类型|非空，选择录入结转单的单据类型名称，点击栏目可以在线维护“单据类型”数据。|
|单号|非空,显示或输入结转单的编号。当用户所选的【单据类型】定义为自动编号时，本栏目显示系统自动生成的单据编号；而【单据类型】定义为手工编号时，请在本栏目中输入单据编号|
|单据日期|系统默认取当前的日期，可改|
|项目|参照项目主档录入；|
|成本分解结构|根据所录入的项目带入，可改，不可为空|
|账簿|参照账簿档案|
|币种|项目成本的核算币种|
|客户|参照客户档案录入；|
|业务员|参照选择业务员档案|
|状态|系统自动生成|
|备注|说明性备注|
|表体|
|行号|行号|
|支出类型|根据所录入的成本分解结构参照|
|项目成本项|根据所录入的支出类型参照|
|存货成本分项|可选择材料、人工、制造、机器、外协、采购等分项|
|本层成本|自动带出|
|本次结转本层成本|手工录入|
|下层成本|自动带出|
|本次结转下层成本|手工录入|
|扩展字段|如果启用了扩展字段，可以录入相应的扩展信息|

# 操作说明

## 卡片画面操作说明

*  【保存】：将当前操作中编辑的内容保存在数据库中。
*  【放弃】：将当前操作编辑内容不进行保存，仍保存原有内容，画面恢复至编辑前状态。
*  【新增】：将当前画面置为空白可编辑状态；并将原画面编辑内容进行保存。
*  【删除】：删除一条已经保存的数据。
*  【复制】：将当前画面录入内容复制到新画面中，画面显示新复制过来的信息。
*  【查找】：弹出查找画面，输入查找条件，过滤已经保存的数据。
*  【列表】：从卡片录入画面切换到列表查询画面。详细操作见《列表画面**操作说明**》。
*  【首页】：显示第一条单据或档案信息。
*  【上页】：显示上一条单据或档案信息。
*  【下页】：显示下一条单据或档案信息。
*  【末页】：显示最后一条单据或档案信息。

## 卡片翻页顺序

*  从卡片的查找画面返回卡片
 
   *  返回的卡片应该是在查找画面中选择的记录，若选择的记录是行记录或者子行记录，则卡片上焦点应该自动定位在对应行上；
   *  卡片的翻页顺序应该根据当前查找方案确定；
*  从列表画面返回卡片：
 
   *  返回的卡片应该是在列表画面中选择的记录，若选择的记录是行记录或者子行记录，则卡片上焦点应该自动定位在对应行上；
   *  卡片的翻页顺序应该根据当前查询方案确定；
*  链接弹出卡片：
 
   *  档案类的链接：直接显示对应档案的卡片信息，按系统默认方案进行翻页；
   *  单据类的链接，直接显示焦点对应单据卡片：
   
      *  如果是从一个列表记录的列标题链接进入卡片的，则在该列表记录间进行翻页；
      *  如果只是一个特定的单据编号进入卡片，则按系统默认方案进行翻页；

翻页方案按行或者子行显示时要求排重后再进行整单翻页；
